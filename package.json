{
  "name": "dubwave",
  "version": "1.2.0",
  "private": false,
  "description": "DubWave browser extension with pluggable LLM endpoints and optional LiveKit transport.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "livekit-client": "^2.22.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
